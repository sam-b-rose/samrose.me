/**
 * This button looks like regular text.
 */
import styled from 'styled-components';

const InvisibleButton = styled.button`
  background: none;
  color: inherit;
  border: none;
  padding: 0 !important;
  font: inherit;
  cursor: pointer;
`;

export default InvisibleButton;
