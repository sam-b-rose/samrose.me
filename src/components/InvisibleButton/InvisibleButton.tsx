import { styled } from '@style';

const InvisibleButton = styled('button', {
  background: 'none',
  color: 'inherit',
  border: 'none',
  padding: '0 !important',
  font: 'inherit',
  cursor: 'pointer',
});

export default InvisibleButton;
